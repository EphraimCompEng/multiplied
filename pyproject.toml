[project]
name = "multipy"
version = "0.0.0"
description = "A powerful tool to research, build, and optimise multipliers."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.0",
]
